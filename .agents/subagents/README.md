# Custom Subagents

Place repository-specific subagent definitions (`.toml`) here. Keep each agent focused on one bounded responsibility, document its inputs and expected output, and avoid embedding secrets.

