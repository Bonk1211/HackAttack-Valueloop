# Local Plugins

Use this directory for repository-scoped plugin assets or documentation. Each plugin should live in its own clearly named directory and include setup and usage instructions. Do not commit generated caches or secrets.

